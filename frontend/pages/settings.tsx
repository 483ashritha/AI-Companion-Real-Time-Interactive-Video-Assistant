import { useEffect, useState } from 'react';
export default function Settings(){ const [captions,setCaptions]=useState(false); useEffect(()=>{ const v=localStorage.getItem('captions'); setCaptions(v==='true'); },[]); function toggle(){ const n=!captions; setCaptions(n); localStorage.setItem('captions', String(n)); } return (<div className='container'><h2 className='text-2xl font-semibold text-indigo-600'>Settings</h2><div className='card mt-4'><label className='font-medium'>Captions</label><div className='mt-2'><button className='btn' onClick={toggle}>{captions? 'Disable':'Enable'}</button><p className='text-sm text-gray-500 mt-2'>Toggle client-side captions via Web Speech API.</p></div></div></div>); }
